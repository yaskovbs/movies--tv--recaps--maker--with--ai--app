# איך המערכת עובדת מאחורי הקלעים

מסמך טכני שמסביר בפירוט איך Movies & TV Recaps Maker בנוי ועובד בפועל - מה קורה בכל שלב, אילו שירותים חיצוניים מעורבים, ואילו החלטות עיצוב (ולפעמים באגים שתוקנו) עומדות מאחורי ההתנהגות הנוכחית. למי שרוצה רק להשתמש באתר או להריץ אותו, [README.md](README.md) מספיק. למי שרוצה להבין/לשנות את הפנימיות - זה המסמך הנכון. לתולדות הפיתוח (מה השתנה ומתי) ראו [CHANGELOG.md](CHANGELOG.md).

## התמונה הגדולה

זו אפליקציית **Single Page Application** (Vite + React + TypeScript) שרצה **כולה בצד הלקוח** - אין שרת Backend עצמאי שכתבנו. שני שירותים חיצוניים בלבד מספקים את מה שדפדפן לבד לא יכול:

- **Google Gemini API** - נקרא ישירות מהדפדפן עם מפתח ה-API של המשתמש. תפקיד יחיד: לצפות בווידאו ולבחור אילו רגעים כדאי לכלול בסיכום.
- **Supabase** (אופציונלי) - Backend-as-a-Service לשמירת סיכומים, היסטוריה, התחברות, וסטטיסטיקות גלובליות. האתר עובד גם בלעדיו (רק שמירה/היסטוריה/סטטיסטיקות אמיתיות לא יפעלו).

עיבוד הווידאו עצמו (החיתוך וההרכבה) קורה **לגמרי בדפדפן** באמצעות FFmpeg.wasm - קובץ הווידאו המקורי אף פעם לא מועלה לשום שרת שלנו.

## זרימת יצירת סיכום, שלב אחרי שלב

כל הלוגיקה נמצאת ב-`src/components/HomePage.tsx`, בפונקציה `handleCreateRecap`.

### 1. טעינת מנוע FFmpeg

בלחיצה הראשונה על "צור סיכום", המנוע (`@ffmpeg/ffmpeg`, בגרסת WebAssembly) נטען פעם אחת מ-CDN (`unpkg.com/@ffmpeg/core@0.12.6`) ונשמר ב-`ffmpegRef` כדי שלא ייטען שוב בסיכומים הבאים באותה הפעלה.

### 2. Gemini "צופה" בווידאו ובוחר רגעים (אם אפשר)

זה קורה **לפני** החיתוך בפועל, לא אחריו - כדי שההחלטה על מה לחתוך תהיה מבוססת על מה שבאמת קורה בסרטון, לא רק על ניחוש עיוור.

1. אם הקובץ מתחת ל-**2GB** (המגבלה של Gemini File API לקובץ בודד - `GEMINI_VIDEO_SIZE_CAP`), הוא מועלה ל-Gemini דרך **resumable upload** (`uploadFileToGemini`): בקשת `POST` שמתחילה את ההעלאה, ואז שליחת הבייטים בפועל.
2. אחרי ההעלאה, הקובץ עובר עיבוד בצד גוגל (חילוץ פריימים וכו') - האתר **סוקר (polling)** כל 2 שניות עד שהקובץ מגיע למצב `ACTIVE`, עם **תקציב המתנה של עד 22 דקות** (סרטים ארוכים לוקחים זמן עיבוד ממשי אצל גוגל, לא רק זמן העלאה - זו הייתה סיבת כשל אמיתית שתוקנה: תקציב קודם של 90 שניות בלבד גרם לכשלים כמעט תמידיים בסרטים אמיתיים).
3. ברגע שהקובץ פעיל, נשלחת בקשת `generateContent` עם הווידאו מצורף (`file_data`) ופרומפט שמבקש מ-Gemini להחזיר מערך JSON של קטעים חשובים בפורמט `[{"start": "HH:MM:SS", "end": "HH:MM:SS"}, ...]`. גם לשלב הזה יש תקציב המתנה נפרד של עד 22 דקות (עם `AbortController`), כי גם הניתוח עצמו יכול לקחת זמן על סרטון ארוך.
4. **אכיפה קשיחה, לא רק בקשה בפרומפט**: כל קטע שמוחזר נחתך בקוד עצמו לכל היותר ל-`captureSeconds` (ברירת מחדל: שנייה אחת), ללא קשר לאורך שג'מיני בפועל החזיר. זו החלטה מכוונת - קטעים קצרים ומפוזרים בטוחים משמעותית מבחינת זכויות יוצרים מאשר קטעים רצופים וארוכים יותר, ולכן זה חייב להיות מחייב (Enforced) ולא רק מוצע.
5. **התאוששות שקטה**: אם ההעלאה נכשלת, הקובץ גדול מדי, הניתוח נכשל, או Gemini חוסם את התשובה (למשל בגלל פילטרי בטיחות) - שום דבר לא קורס. המערכת פשוט ממשיכה בלי קטעים "חכמים" ונופלת לחיתוך תקופתי רגיל בשלב הבא.

**חשוב להבין**: Gemini **לא כותב תסריט קריינות יותר** (התכונה הזו הוסרה לגמרי לפי בקשה מפורשת). התפקיד היחיד שלו הוא בחירת הרגעים לחיתוך.

### 3. בניית פילטר החיתוך והרכבת הסרטון עם FFmpeg

```
selectFilter = 
  אם יש קטעים חכמים:  select='between(t,s1,e1)+between(t,s2,e2)+...',setpts=N/FRAME_RATE/TB
  אחרת (נפילה חזרה):   select='lt(mod(t,intervalSeconds),captureSeconds)',setpts=N/FRAME_RATE/TB
```

FFmpeg מריץ `select` filter שבורר רק את הפריימים בטווחי הזמן הרלוונטיים, מחבר אותם ברצף (`setpts`), ומקודד מחדש:
- רזולוציה מוגבלת ל-1280 פיקסלים לרוחב (`scale='min(1280,iw)':-2`) - סיכומים לא צריכים רזולוציית מקור מלאה
- קידוד `libx264` בפריסט `veryfast` ו-`crf 26` - קיצור זמן עיבוד משמעותי לעומת ברירת המחדל
- אם המשתמש העלה קובץ קריינות MP3 משלו - הוא ממופה כערוץ האודיו של הפלט (`-map 1:a`); אחרת הפלט שקט (`-an`)
- `-t {duration}` חותך בדיוק לאורך המבוקש; `-movflags +faststart` מאפשר נגינה מיידית בדפדפן

**חשוב**: הקובץ שנקרא בפועל הוא ה-buffer שנשמר בזמן בחירת הקובץ (`selectedFile.buffer`), ולא ה-`File` object המקורי - קריאה מה-`File` המקורי בשלב מאוחר יכולה להיכשל עם `NotReadableError` אחרי אינטראקציות מסוימות של הדפדפן.

### 4. תוצאה

ה-`Blob` שנוצר (לא רק ה-`blob:` URL הנגזר ממנו) נשמר ב-state, כי `blob:` URL תלוי ברישום פנימי של הדפדפן שיכול "להתפוגג" (בעיקר במובייל, תחת לחץ זיכרון) עוד לפני שהאובייקט עצמו היה נאסף לזבל. שמירת ה-`Blob` בפועל עוקפת את כל מחלקת התקלות הזו.

## Gemini - הגדרות בטיחות

כל קריאות Gemini משתמשות ב-`safetySettings` מרוככים (`BLOCK_ONLY_HIGH` בארבע הקטגוריות הניתנות לכיוונון: הטרדה, שנאה, מיני, מסוכן) - כי סרטים וסדרות באופן לגיטימי כוללים אלימות, פשע ואימה כחלק מהז'אנר עצמו, וסף ברירת המחדל של גוגל (`BLOCK_MEDIUM_AND_ABOVE`) חוסם לעיתים קרובות מדי תיאורי עלילה רגילים.

**מגבלה אמיתית שאי אפשר לעקוף**: `PROHIBITED_CONTENT` הוא סינון מובנה של גוגל שלא ניתן לכיוונון בכלל דרך ה-API (מיועד לתוכן שחוצה קו אדום מוחלט, כמו פגיעה בקטינים) - גם עם `safetySettings` הכי מקלים אפשר, זה עדיין ייחסם. כשזה קורה, האתר מציג הודעה שמסבירה את זה במפורש במקום לרמוז שאפשר לכוונן משהו.

## שכבת הנתונים - Supabase

### התחברות

כל מבקר מקבל אוטומטית **משתמש אנונימי אמיתי** (`supabase.auth.signInAnonymously()`) - לא רק מזהה שנוצר בצד הלקוח, אלא `auth.uid()` אמיתי שמזוהה בצד השרת ושמדיניות ה-RLS (למטה) בודקת מולו. מי שבוחר "להירשם" בפועל רק משדרג את אותו session קיים (`supabase.auth.updateUser({email, password})`) - אותו `uid`, ההיסטוריה עוברת אוטומטית, בלי תהליך הרשמה נפרד.

### טבלאות (`supabase/migrations/`)

| טבלה | תפקיד | RLS |
|---|---|---|
| `recaps` | סיכומים שנשמרו (וידאו/אודיו URLs, תיאור, דירוג) | לפי `auth.uid() = user_id` |
| `tuning_jobs` | תשתית ל-fine-tuning אישי של Gemini (ראו הערה למטה) | לפי `auth.uid() = user_id` |
| `app_stats` | שורה יחידה - סה"כ סיכומים, סכום/מונה דירוגים | קריאה פתוחה לכולם, כתיבה רק דרך RPC |
| `stats_visitors` | UUID אחד לכל מבקר ייחודי | בלי מדיניות בכלל - חסום לגמרי מגישה ישירה |

**עדכון סטטיסטיקות דרך RPC, לא כתיבה ישירה**: הפונקציות `increment_recaps_created()`, `add_app_rating()`, `register_visitor()`, `get_public_stats()` הן `SECURITY DEFINER` - כלומר הלקוח לעולם לא כותב ישירות לטבלה (רק "+1", לא "קבע ל-999999"), וספירת המבקרים הייחודיים (`active_users`) מחושבת "on the fly" מתוך `count(*)` על `stats_visitors` בלי לחשוף את הטבלה עצמה.

### אחסון קבצים

Bucket ציבורי בשם `recaps`. **בכוונה בלי מדיניות SELECT על `storage.objects`** - כי כשה-bucket כבר public, שליפת קובץ בודד ב-URL עובדת בלי צורך ב-RLS בכלל. מדיניות SELECT רחבה הייתה מאפשרת לכל לקוח (גם לא מחובר) לקרוא ל-API של רשימת קבצים ולקבל רשימה מלאה של כל מה שהועלה אי פעם - זו הייתה אזהרת אבטחה אמיתית של Supabase שתוקנה.

### fine-tuning אישי (`src/lib/geminiTuning.ts`) - הערת מצב

זו תשתית ישנה שנבנתה כשעדיין הייתה יצירת תסריטים - איסוף דוגמאות מדורגות כ"טובות" לצורך אימון מודל Gemini מותאם אישית. מאז שהוסרה יצירת התסריטים, **אין יותר תוכן חדש שמזין את זה** - אבל התשתית (טבלה, כפתורי דירוג בעמוד ההיסטוריה, כרטיס "מודל AI אישי") נשארה במקום בכוונה, כי היא עדיין פועלת באופן משמעותי על סיכומים ישנים ששמורים מלפני השינוי. זה לא קוד שבור - רק תכונה שכבר לא צוברת נתונים חדשים.

## עמידות (Resilience) - נפילה חזרה בכל שכבה

עיצוב חוזר לאורך כל האפליקציה: **תמיד יש מה להראות, גם כשמשהו נכשל**.

- **`createClient('', '')` היה קורס את כל האתר** אם משתני הסביבה לא מוגדרים - `supabase.ts` תמיד יוצר לקוח עם URL זמין (אמיתי או placeholder תקין תחבירית) ומסמן `isSupabaseConfigured`, כך שהאתר תמיד נטען.
- **סטטיסטיקות (`src/lib/stats.ts`)**: מנסה קודם Supabase; אם זה נכשל (לא מוגדר, migration לא רץ, בקשה נכשלה) - נופל אוטומטית למונה מקומי ב-`localStorage`, כך שתמיד יש מספרים אמיתיים להציג (גם אם לא משותפים בין משתמשים).
- **דירוג לייק/דיסלייק על תוצאה** מיד אחרי היצירה - משתמש באותו מנגנון `addRating`/`hasRated` הגלובלי, אות חד-פעמי לדפדפן (מי שדירג פה כבר "מיצה" את הדירוג ולא יתבקש שוב בכוכבים של עמוד הבית, ולהפך).
- **בחירת קטעים חכמה** נכשלת בשקט לחיתוך תקופתי, כמתואר למעלה.

## עיבוד וידאו - מגבלות מכוונות

- **3.5GB מקסימום** להעלאה - לא שרירותי: WebAssembly מוגבל ל-4GB זיכרון, ו-3.5GB משאיר מרווח ביטחון.
- **ליבת מעבד אחת בלבד** בכוונה תחילה - עיבוד מרובה-ליבות היה מיושם בעבר ובוטל, כי זה דורש הגדרות COOP/COEP ברמת השרת שיוצרות סיכון תאימות עם משאבים חיצוניים (כמו CDN של FFmpeg).

## בינלאום (i18n)

6 שפות (עברית, אנגלית, רוסית, ערבית, ספרדית, צרפתית) עם `i18next` + זיהוי שפת דפדפן אוטומטי; עברית וערבית מקבלות RTL מלא (`document.documentElement.dir` מתחלף אוטומטית). כל שינוי טקסט חדש חייב תרגום זהה בכל 6 קבצי `src/locales/*.json` - נבדק תמיד באמצעות השוואת מפתחות שטוחה (flatten) בין הקבצים.

## פריסה

Cloudflare Pages, בנייה סטטית (`npm run build` → `dist/`). משתני הסביבה של Supabase נאפים בזמן ה-build (לא runtime) - שינוי שלהם בלוח הבקרה דורש build חדש כדי להיכנס לתוקף בפועל. הקוד מקבל את שמות המשתנים בשתי צורות (`VITE_SUPABASE_*` וגם `NEXT_PUBLIC_SUPABASE_*`) כדי להתאים למה שה-"Connect" של Supabase עצמו נותן.

## תיקיית `desktop-app/`

גרסת Electron נפרדת שמריצה FFmpeg **native** (לא WASM) למהירות גבוהה יותר על קבצים גדולים/ארוכים. **חשובה הבחנה**: זו תיקייה נפרדת לחלוטין בענף הפיתוח, ומעולם לא מוזגה ולא אמורה להיות מוזגת ל-`main` - זו החלטה מכוונת, לא פספוס.
